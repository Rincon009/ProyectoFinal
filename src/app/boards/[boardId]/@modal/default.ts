/**
 * Componente por defecto que no renderiza nada.
 * 
 * Este componente React devuelve `null`, por lo que no muestra ningún contenido
 * en la interfaz de usuario.
 * 
 * @returns {null} - No renderiza ningún contenido.
 */
export default function Default() {
  return null;
}
