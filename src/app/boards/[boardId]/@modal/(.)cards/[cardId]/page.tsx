import CardModal from "@/components/views/CardModal";

/**
 * Página que renderiza el componente CardModal.
 * 
 * Esta función devuelve el componente CardModal, que probablemente muestra
 * una tarjeta modal en la interfaz de usuario.
 * 
 * @returns {JSX.Element} - El componente CardModal.
 */
export default function CardModalPage(): JSX.Element {
  return (
    <CardModal />
  );
}
