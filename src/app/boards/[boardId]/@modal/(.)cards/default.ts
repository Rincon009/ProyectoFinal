/**
 * Componente por defecto que no renderiza nada.
 * 
 * Este componente React devuelve `null`, por lo que no muestra ningún contenido
 * en la interfaz de usuario. Puede ser utilizado como un marcador de posición
 * o en situaciones donde no se requiere renderizar nada.
 * 
 * @returns {null} - No renderiza ningún contenido.
 */
export default function Default(): null {
  return null;
}
